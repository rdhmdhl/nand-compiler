#ifndef ENUMS_H
#define ENUMS_H
	
enum TokenType {
	KEYWORD,
	SYMBOL,
	IDENTIFIER,
	INT_CONST,
	STRING_CONST
};

enum KeywordType {
	CLASS, METHOD, FUNCTION,
	CONSTRUCTOR, INT, BOOLEAN,
	CHAR, VOID, VAR, 
	STATIC, FIELD, LET,
	DO, IF, ELSE,
	WHILE, RETURN, TRUE,
	FALSE, THIS, NIL
};

#endif
